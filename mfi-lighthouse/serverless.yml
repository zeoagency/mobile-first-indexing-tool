service: mfi-lighthouse

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
  timeout: 900
  memorySize: 1024

functions:
  hello:
    handler: handler.hello
    description: MFI Checker Lighthouse - zeo.org
